.header-container.container
  .header-row
    .col-xs-4.logo
      h1
        img.logo(src='assets/images/logo.svg', alt="#grubup")
    .col-xs-8.search
      .pull-right
        input.search-field(type='text', placeholder='zip code')
        input.search-button(type='submit', value='Go')

map#hero-map(center="current-location" geo-fallback-center="40.44 -79.95", zoom="13")
  marker(ng-repeat="marker in markers", position="marker.position")

.body-container.container
  p Blah blah blah